name: Direct Docker Push

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/docker-direct.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: justininstabids
        password: dckr_pat_hCWY83pT8BWzna3Ztpd0YLx0Fec
    
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: justininstabids/sales-discovery-bot:latest
        
    - name: Summary
      run: |
        echo "## Docker Image Published! 🚀" >> $GITHUB_STEP_SUMMARY
        echo "Image: \`justininstabids/sales-discovery-bot:latest\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Run with:" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
        echo "docker run -d \\" >> $GITHUB_STEP_SUMMARY
        echo "  --name sales-discovery-bot \\" >> $GITHUB_STEP_SUMMARY
        echo "  -p 8000:8000 \\" >> $GITHUB_STEP_SUMMARY
        echo "  -e REDIS_URL=redis://host.docker.internal:6379/0 \\" >> $GITHUB_STEP_SUMMARY
        echo "  -e OPENAI_API_KEY=your_key \\" >> $GITHUB_STEP_SUMMARY
        echo "  justininstabids/sales-discovery-bot:latest" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
